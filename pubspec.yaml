name: myapp
description: "A new Flutter project."

# Prevent accidental publishing to pub.dev
publish_to: 'none' # Remove if you wish to publish

# Versioning
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  intl: ^0.18.0 # Ensure this is added for number formatting

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Add image assets
  assets:
    - assets/images/image-1.jpg
    - assets/images/image-2.jpg
    - assets/images/image-3.jpg
